<VirtualHost *:80>
  ServerAdmin buzzcauldron@users.noreply.github.com
  DocumentRoot /var/www/visual-page-editor

  <Directory "/var/www/visual-page-editor">
    Options -Indexes

    RewriteEngine On

    RewriteRule \.sh$ - [R=404]

    RewriteCond /var/www/visual-page-editor/%{REQUEST_URI} -f
    RewriteCond %{REQUEST_URI} !.*\.(php|css|js|xml)
    RewriteRule . /app/authGetFile.php?b=/var/www/visual-page-editor&f=%{REQUEST_URI} [L]
  </Directory>

  LogFormat "%h %l %{uname}n %t \"%r\" %>s %b \"%{Referer}i\" \"%{User-agent}i\"" common_with_php_username
  CustomLog ${APACHE_LOG_DIR}/access.log common_with_php_username
  ErrorLog ${APACHE_LOG_DIR}/error.log
</VirtualHost>
